[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "teacherlm-mcp-server"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "httpx>=0.28",
    "pydantic>=2.12"
]

[tool.hatch.build.targets.wheel]
packages = ["teacherlm_mcp_server"]

