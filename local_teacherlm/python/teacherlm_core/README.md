# TeacherLM Core, Local Edition

Shared contracts and local retrieval utilities for the desktop rewrite.

Compatibility rules:

- Python 3.14+
- Pydantic V2 only
- no LangChain or LangGraph
- no `llama-parse` or `llama-cloud-services`
- use provider adapters directly, including Ollama's native `format=` argument

