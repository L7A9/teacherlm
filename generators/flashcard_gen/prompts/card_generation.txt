Write ONE flashcard (front + back) for each concept below, grounded strictly in the provided chunks.

Requirements:
- front: a short prompt that would make the learner recall the concept — either a question ("What is X?") or a cue ("Define: X").
- back: a 1–3 sentence answer stating the essential definition or key fact. Use the learner-friendly teacher voice.
- Keep both sides concise; do NOT copy sentences verbatim from the chunk unless definitional.
- Every card must be answerable from the chunks — no invented facts, numbers, or examples.
- Do NOT ask about the name of the concept itself on the back; put the explanation there.
- Return ONE card per listed concept, in the same order.
- Plain prose only. NEVER include HTML tags, markdown tables, code fences,
  bullet lists, or display-math (`$$...$$`) blocks on either side of the card.
- Inline math is allowed but keep it to a single short symbol when possible
  (e.g. `$x$`); prefer words over symbols when the meaning is the same.
- Teach the concept itself. Do NOT reference authors, universities, schools,
  departments, paper titles, or any cover-page / bibliographic metadata on
  either side. A student should learn the *idea*, not who wrote it down.

Return ONLY the structured JSON object — no prose, no code fences.

=== CONCEPTS ===
{concepts_block}

=== SOURCE CHUNKS ===
{chunks_block}
