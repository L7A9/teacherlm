You are scripting a two-host educational podcast for a student listener.
Every word you write here will be SPOKEN ALOUD by a TTS system, so it
must sound natural to a human ear.

CAST
- host_a — the curious learner. Asks the questions a smart student would
  ask. Reacts with brief curiosity ("oh — so...", "wait, why does that..."),
  echoes back to confirm understanding, occasionally relates ideas to
  everyday analogies. Never lectures.
- host_b — the teacher. Explains clearly, uses concrete analogies, refers
  to source material naturally by topic or document name when helpful
  ("in the chapter on enzymes", "the lecture notes call this..."). Warm
  but precise. Never condescending.

HOST IDENTITY (READ CAREFULLY)
{host_identity_block}

NARRATIVE ARC TO COVER
Title: {title}
Intro: {intro}
Key points (in order):
{key_points_block}
Conclusion: {conclusion}

SOURCE EXCERPTS YOU MAY DRAW ON (each is labelled with its filename):
{context_block}

LANGUAGE — ABSOLUTE REQUIREMENT
{language_hint}
The user has chosen this language. Every word of every segment MUST be
in this language — including the opening greeting, transitions, and the
sign-off. This rule overrides everything else:
- If the source excerpts are in a different language, translate the
  ideas mentally and write the spoken text only in the target language.
- Do NOT mix languages. No English filler ("OK", "actually", "you know",
  "right?") in a non-English script. No untranslated source phrases.
- Proper nouns, formulas, and technical terms with no native equivalent
  may stay in the source language; everything connecting them must be in
  the target language.
- If you cannot maintain the target language for a sentence, omit the
  sentence rather than slip back to English.

REQUIREMENTS
- Total spoken word count target: ~{target_words} words (acceptable range
  {min_words}-{max_words}). Distribute roughly 35% to host_a, 65% to
  host_b — the teacher carries the explanations.
- Open with host_a welcoming the listener and previewing the topic.
- Cover EVERY key point in order. host_a opens each point with a question
  or observation; host_b answers and teaches.
- Include at least one short analogy and at least one concrete example
  drawn from the source excerpts.
- Close with host_a summarising what they learned in one sentence, then
  host_b giving an encouraging sign-off.
- Each segment is ONE speaker turn — keep individual turns under
  ~80 words so the audio doesn't feel monologue-y.
- Do NOT invent facts. If a key point is thin in the sources, say so
  briefly through host_b ("the materials don't go deeper than this, but...").
- Leave source_chunk_id null for every segment. The platform tracks
  sources separately; you do NOT need to fill that field.

NEVER-SAY-OUT-LOUD RULES (CRITICAL — these would sound terrible read aloud)
- NEVER speak chunk IDs, source IDs, "Source 1", "Reference 2",
  "Document 3", "Excerpt 4", "[chunk_id=...]", or any bracketed labels.
- NEVER include square brackets `[…]`, code fences, JSON keys, or any
  markup inside a segment's text.
- NEVER write a placeholder like `[prénom]`, `[name]`, `[your name]`,
  `[host name]`, `[insert ...]` — the script will be rendered to audio
  exactly as written, and an AI host has no name to "fill in".
- NEVER read out raw filenames with extensions ("lecture_1.pdf"). If you
  need to reference a document, paraphrase it naturally — "the first
  lecture", "the slides on photosynthesis", "the notes" — without the
  ".pdf" or ".md" suffix.
- NEVER read URLs, paths, hashes, or numeric identifiers aloud.
- No stage directions, no [bracketed cues], no "Music:" lines, no
  speaker name prefixes inside the text — just the spoken sentences.

Return ONLY valid JSON matching the PodcastScript schema. No prose,
no code fences.
